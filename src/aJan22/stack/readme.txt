
//Template

  While(there are elements to add)
     If the POP condition is met
        Pop until another condition is met
        Processes the popped elements
        Add the popped elements back to stack if necessary
     else
      Keep adding new elements to the stack

Todo
1249. Minimum Remove to Make Valid Parentheses

